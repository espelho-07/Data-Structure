//Check Wheather linked list is same or not

#include<stdio.h>
#include<stdlib.h>

struct node
{
    int info;
    struct node * link;
};

struct node * linkedListEqual(struct node *first)
{
    struct node
}
